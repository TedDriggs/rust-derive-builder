// extern crate serde;

// #[macro_use]
// extern crate serde_derive;

// #[macro_use]
// extern crate derive_builder;

// #[derive(Debug, Builder)]
// #[serde(rename = "hi")]
// #[builder(forward(serde))]
// struct Lorem {
//     #[serde(rename = "foo")]
//     ipsum: u16
// }

fn main() {
    // The example is commented out to avoid taking a dependency on serde.
    assert!(true);
}